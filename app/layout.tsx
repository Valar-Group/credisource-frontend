import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'CrediSource - Know What\'s Real',
  description: 'AI content verification platform. Verify images, videos, and text for authenticity.',
  keywords: 'AI detection, deepfake detection, content verification, fake news detection',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
